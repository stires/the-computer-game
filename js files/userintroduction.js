/*greeting if user is not registered
shouldn't be too long */

function greeting (name = 'stranger') {
    console.log(`Hello, ${name}!`)
  }
   
  greeting('new user!') // output- hello, new user!
  greeting() //output- hello!

/* i may tweak this a couple times
but this can keep us up for now. im happy with this :) */
